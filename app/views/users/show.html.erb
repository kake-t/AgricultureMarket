<%# ユーザー詳細画面 %>
<div class="container">

<%# user のデータ %>
<%= render 'users/params', user: @user %>

  <% if current_user == @user %>
    <div class="">
      <%= link_to 'ユーザー情報編集', edit_user_registration_path %>
    </div>
  <% end %>

  <div class="">
    <%# 生産者情報を持っている時、生産者情報を表示する %>
    <% unless @user.producer.nil? %>
      <div class="">
        <p>生産者情報</p>
        <%= render 'producers/params', producer: @producer %>
      </div>
    <% end %>
  </div>

  <%= render 'users/links' %>

</div>
