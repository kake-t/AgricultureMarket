<div class="container">
  <%= form_with model: @item, local: true do |form| %>
    <div class="">
      <%= image_tag @item.item_image.url %>
    </div>

    <div class="">
      <%= form.label :item_name %>
      <%= form.hidden_field :item_name %>
      <%= @item.item_name %>
    </div>

    <div class="">
      <%= hidden_field_tag :"cache[item_image]", @item.item_image.cache_name %>
    </div>

    <div class="">
      <%= form.label :price %>
      <%= form.hidden_field :price %>
      <%= @item.price %>
    </div>

    <div class="">
      <%= form.label :item_content %>
      <%= form.hidden_field :item_content %>
      <%= @item.item_content %>
    </div>

    <div class="">
      <%= form.submit 'はい' %>
    </div>
  <% end %>

<% #戻るボタン %>
  <div class="">
    <%= form_with model: @item, local: true, url: new_item_path, method: :get do |form| %>
      <%= form.hidden_field :item_name %>
      <%= form.hidden_field :price %>
      <%= form.hidden_field :item_content %>
      <%= form.submit 'いいえ', name: :back %>
    <% end %>
  </div>
</div>
